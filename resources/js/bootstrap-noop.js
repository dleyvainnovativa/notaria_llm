// Intentionally empty. The prototype uses native fetch (see app.js), so we
// don't pull in axios or Laravel's default bootstrap. Kept as a named import
// so app.js structure matches a normal Laravel entry without extra deps.
